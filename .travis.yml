---
notifications:
  email:
    on_success: never
    on_failure: change

sudo: true

language: python
python:
  - '2.7'
  - '3.6'

install:
  - pip install tox-travis coveralls
  - python setup.py install

script:
  - tox

after_script:
  - coveralls
